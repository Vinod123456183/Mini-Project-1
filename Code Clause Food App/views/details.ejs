<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= recipe.name %> - Details</title>
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
    <script src="https://cdn.tailwindcss.com"></script>

    <style>
        body {
            background-color: #efefef;
        }
    </style>
</head>
<body class="bg-gray-100">

    <div class="flex p-3 items-center bg-gray-100 justify-between px-6 lg:px-10">
        <div><p class="font-bold text-md lg:text-xl   text-red-500"><a href="/">Food<span class="text-black">King</span</a></p></div>
        <div class="flex text-xs lg:text-lg  items-center gap-3 lg:gap-5">
            <p class="hover:text-blue-500"><a href="/">Home</a></p>
            <p class="hover:text-blue-500"><a href="/recipies">Recipies</a></p>
            <p class="hover:text-blue-500"><a href="/add">Add</a></p>
        </div>
    </div>

    

    <div class="max-w-xl mx-auto p-4 bg-white rounded-lg shadow-md mt-4">
        <!-- Recipe Image -->
        <div class="mb-4">
            <img src="<%= recipe.imageUrl %>" alt="<%= recipe.name %>" class="w-full h-64 object-cover rounded-lg shadow-sm">
        </div>

        <!-- Recipe Details -->
        <div class="flex justify-between items-center mb-6">
            <div class="flex-1 text-center">
                <p class="text-3xl font-semibold text-gray-800"><%= recipe.name %></p>
            </div>
        </div>

        <div class="flex justify-between mb-6 text-center text-gray-800">
            <!-- Ingredients Count -->
            <div class="flex flex-col items-center">
                <p class="text-2xl font-bold text-green-500">9</p>
                <p class="text-lg mt-1 text-gray-600">Ingredients</p>
            </div>
            <!-- Duration -->
            <div class="flex flex-col items-center">
                <p class="text-2xl font-bold text-blue-500"><%= recipe.duration %></p>
                <p class="text-lg mt-1 text-gray-600">Minutes</p>
            </div>
            <!-- Calories -->
            <div class="flex flex-col items-center">
                <p class="text-2xl font-bold text-red-500"><%= recipe.calories %></p>
                <p class="text-lg mt-1 text-gray-600">Calories</p>
            </div>
        </div>

        <!-- Recipe Category -->
        <div class="mb-4 text-center">
            <p class="bg-yellow-500 text-white py-1 px-3 rounded-full inline-block font-semibold">
                Indian
            </p>
        </div>

        <!-- Edit Recipe Button -->
        <div class="mb-6 text-center">
            <a href="/edit?name=<%= recipe.name %>" class="bg-blue-600 text-white px-4 py-2 rounded-full font-semibold hover:bg-blue-700 transition duration-300">
                Edit Recipe
            </a>
        </div>

        <!-- Ingredients List -->
        <div class="px-6 py-3">
            <p class="text-lg font-semibold text-gray-700">Ingredients:</p>
            <p class="mt-2 text-gray-600 border-t-2 pt-2 text-base"><%= recipe.ingredients %></p>
        </div>

    </div>

</body>
</html>
